package com.mobdeve.s21.pokesoul.model

class Pokemon(name: String, status: String, level: Int) {

    var name = name
        private set

    var status = status
        private set

    var level = level
        private set
}